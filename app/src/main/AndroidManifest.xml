<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bluetooth_kotlin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_base"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_base"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bluetooth_Kotlin">
        <activity
            android:name=".BossGameActivity"
            android:exported="false" />
        <activity
            android:name=".LocalGameActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>